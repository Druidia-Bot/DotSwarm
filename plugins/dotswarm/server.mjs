#!/usr/bin/env node
// Plugin entry point referenced by .mcp.json (Claude Code) and .codex-mcp.json (Codex). The plugin is self-contained and
// dependency-free at runtime; generated files go to the DotSwarm data directory.
import './src/server.mjs';
